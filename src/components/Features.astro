---
const FEATURES = [
  {
    icon: 'ti ti-link',
    title: 'Acortamiento Instantáneo',
    description: 'Transforma URLs largas en enlaces cortos y elegantes en milisegundos',
    color: 'purple',
  },
  {
    icon: 'ti ti-qrcode',
    title: 'Códigos QR',
    description: 'Genera códigos QR únicos para cada enlace con un solo click',
    color: 'blue',
  },
  {
    icon: 'ti ti-shield',
    title: 'Protección con Contraseña',
    description: 'Asegura tus enlaces importantes con autenticación por contraseña',
    color: 'purple',
  },
  {
    icon: 'ti ti-tag',
    title: 'Seguimiento',
    description: 'Identifica tus URL con tags personalizados',
    color: 'blue',
  },
]
---

<section id="features" class="relative z-10 mx-auto max-w-7xl px-6 py-8">
  <div class="mb-16 text-center">
    <h2 class="mb-4 text-4xl md:text-5xl">Características</h2>
    <p class="text-xl text-slate-400">
      Todo lo que necesitas para administrar y optimizar tus enlaces
    </p>
  </div>

  <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-2">
    {
      FEATURES.map((feature) => (
        <div class="group cursor-pointer rounded-[12px] border border-white/10 bg-white/5 p-6 backdrop-blur-sm transition-all hover:scale-105 hover:bg-white/10">
          <div
            class={`h-12 w-12 rounded-xl bg-linear-to-br ${
              feature.color === 'purple'
                ? 'border border-purple-500/30 from-purple-500/20 to-purple-600/20'
                : 'border border-blue-500/30 from-blue-500/20 to-blue-600/20'
            } mb-4 flex items-center justify-center`}
          >
            <i
              class={`${feature.icon} iteam-center h-6 w-6 text-2xl ${feature.color === 'purple' ? 'text-purple-500' : 'text-blue-500'}`}
            />
          </div>
          <h3 class="mb-2 text-xl">{feature.title}</h3>
          <p class="text-slate-400">{feature.description}</p>
        </div>
      ))
    }
  </div>
</section>
